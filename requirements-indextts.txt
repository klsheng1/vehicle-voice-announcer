# Optional heavy dependencies for the real IndexTTS2 engine.
# Install ONLY if you want actual neural voice cloning:
#   pip install -r requirements-indextts.txt
#
# Recommended: install torch matching your CUDA build first, see
# https://pytorch.org/get-started/locally/
torch>=2.3
torchaudio>=2.3
# IndexTTS2 itself — install from the official repository:
#   git clone https://github.com/index-tts/index-tts.git
#   pip install -e ./index-tts
accelerate>=0.95
transformers>=4.43
tokenizers>=0.19
